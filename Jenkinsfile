pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo "Welcome to Jenkins Enviroment"
                sh 'echo usning shell within jenkins file'
                echo 'not using shell in jenkinsfile'
            }

        }

    }
}